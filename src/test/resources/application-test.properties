# Test Configuration
spring.application.name=wms-test

# R2DBC connection URL for H2 in-memory database (no PostgreSQL mode to avoid case-sensitivity issues)
spring.r2dbc.url=r2dbc:h2:mem:///wmstestdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
spring.r2dbc.username=sa
spring.r2dbc.password=

# Flyway Configuration
spring.flyway.user=sa
spring.flyway.password=
spring.flyway.url=jdbc:h2:mem:wmstestdb;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE

# Logging
logging.level.mk.dmt.wms=DEBUG
logging.level.org.springframework.r2dbc=DEBUG
logging.level.org.flywaydb=DEBUG
